# 2026-09-12 — CM module (LLM target grounding)

## Implemented
- New `project/src/CM/` (Python): `candidates.py` (L3 spots per object x free side: corner_a/center/corner_b,
  hull-collision filtered, compass + nearest-landmark description, one spot inside the door),
  `prompts/grounding.json` (editable system/rules/schema/few-shot), `llm_client.py` (openai | anthropic | fake,
  common `complete(system, messages, schema) -> dict`), `dialog.py` (GroundingSession: validator + 1 retry,
  wildcard ids `obj_1:*`, auto-fill single options, code decides ask/goal/none from slot state),
  `app.py` (Flask :5001), `static/index.html` (renderer + chat, pink = possible spots, green hull = goal),
  `test_grounding.py` (200-seed collision test, bed funnel, autofill, validator, none — all pass), `README.md`.

## Decisions (with user)
- Module lives in `project/src/CM`, not `simulation/` (user, 2026-09-12). Scenes are still read/written in
  `simulation/room/scenes/` so both apps share rooms.
- LLM handles words only: it picks candidate ids from a code-generated list; every coordinate comes from
  `candidates.py`. Screen words (top/right) are mapped to compass (north/east) in the prompt.
- Highlight only — no goal is sent to the robot yet.
- Both providers wired (user did not choose); default `CM_PROVIDER=openai` because that SDK is installed.
  Anthropic path: `claude-opus-5`, `beta.messages.create` + `output_config` JSON schema + `fallbacks="default"`.
- Reviews: standards 6 fixed, logic 3 fixed (see task execution log).

## Pending
- User to run `python project/src/CM/app.py` with an API key and confirm the bed dialog (task status `testing`).
- Live OpenAI/Anthropic calls not exercised in this session (no key on this PC).
- Follow-ups in `agent/plan/cm_plan.md` (send goal to robot, scenario suite, local model).

## Interface changes
- New CM response contract (`/api/ground/say`): `{type: ask|goal|none|error, text, slots{target,side,spot},
  matches[{id,x,y,theta,description,...}], resolved}` — units m/rad, world frame. Documented in CM README.
- Candidate id format `target:side:spot` is the L2 -> L3 handle (replaces roadmap §3.2 `relation` field for
  Phase 1; roadmap doc not updated yet).
