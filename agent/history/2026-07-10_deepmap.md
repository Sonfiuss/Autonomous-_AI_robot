# Session log — 2026-07-09 tối → 2026-07-10 — deepmap (visual odometry + anchor hardening)

## Implemented
1. **deepmap/visual_odom.py (MỚI)** — VO giữa các stop: ORB trên ảnh
   right-rectified, nâng keypoint lên 3D metric trong frame đã level,
   RANSAC planar (yaw + XZ). VOTracker gate theo inlier/rms/scale-ratio/
   sanity-bound với lệnh động cơ; fallback chain bước dead-reckon tương đối.
   Wire vào FusionMapper + sequential; CLI --no-vo, --vo-min-inliers.
2. **--replay** trong stereo_walk_map: chạy lại pipeline từ shot đã lưu.
3. **level_to_floor**: xoay TRƯỚC theo tilt ước lượng per-frame
   (info['tilt_deg']), plane-fit chỉ tinh chỉnh; fit lệch >20° → tilt-only
   + floor_y = median điểm đáy khung.
4. **stereo_ruler.floor_anchors**: golden-veto — golden trong dải trung tâm
   có Z_stereo < 0.6×Z_sàn(hàng) = vật cản → cắt mọi hàng anchor phía trên
   chân nó (sàn bị che).
5. **stereo_ruler.fuse_metric**: trust_beyond=1.5 — pixel ngoại suy sâu hơn
   1.5× bằng chứng metric xa nhất → UNRESOLVED (lỗ trống thay vì tường giả).

## Validation (3 bộ ảnh pathological trong output/stereo_walk)
- VO bắt đúng scale lệch 21% giữa stop 0/1 (SCALE x0.79 → hard reject).
- --test regression PASS 2 mode; VO đo d≈0 trên ảnh lặp (1999/1999 inl) →
  tự loại theo bound. Baseline mới: golden=39 inl=3 rms=0.0637 anch=5
  Z[0.38,1.42].
- Per-stop clouds (output/stereo_walk_vo/stop_N.ply + preview PNG):
  cam_h 0.29/0.29/0.29 — scale sàn gần đồng nhất giữa 3 frame
  (trước fix: 0.15/0.11/0.06).

## Giới hạn đã xác nhận (không phải bug)
- Cảnh không có texture xa ⇒ không có bằng chứng metric xa ⇒ vùng xa bị
  nén (tường 2m → 0.8m) — chỉ bộ ảnh mới ở cảnh thoáng sửa được.
- Nén scale ĐỒNG ĐỀU 2 frame thì VO scale-check không thấy (ratio=1),
  chỉ sanity bound đỡ.
- fit_floor_plane sample không seed → frame borderline đổi mode
  plane-fit/tilt-only giữa các lần chạy.

## Pending
- Task 2026-07-09_visual-odometry-align: status testing — chờ user chạy
  hardware walk cảnh thoáng (đo cam height trước), A/B --no-vo.
- Task 2026-07-09_walk-scale-fit: step 7 (cùng lần chạy hardware đó);
  baseline trong task đã outdated, xem NOTE trong file.

## Interface changes
- level_to_floor trả (pts, cam_h, fit_ok, lvl) — thêm tham số tilt_deg.
- floor_anchors thêm kwarg golden; fuse_metric thêm kwarg trust_beyond,
  info thêm 'z_trust'.
