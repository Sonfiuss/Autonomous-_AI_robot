# Session 2026-09-26 (evening) — vision: camera offset, map video, streaming budget

Task: `agent/tasks/2026-09-25_drive-map.md` (status `executing`), sub-steps 10b, 10c, 14 added on the
user's request at 18:30 and done. Test run: `vision/output/20260926_173930` (turn left 180°, right 180°).

## Implemented
- **Lateral camera offset** — `CameraMount` gained `left_m` (namedtuple default 0, so recording.json /
  camera_mount.json written before it still load). `DEFAULT_MOUNT` = height 0.24, pitch −0.92,
  forward **0.185**, left **+0.062**. Threaded through `floor_hit`, `floor_project`, `camera_to_body`,
  `box_floor_range`, `floor_trapezoid`, `fit_floor`, `frame_motion.floor_rows`; `--cam-left` on record.py,
  calib_floor.py, map_builder.py; `--cam-forward/--cam-left` on drive_map.py and stream_bench.py.
- **Turn axis from the images** — `drive_map.rotation_axis(motion)`: fixed point (I − R)⁻¹t of every
  turning odometry step, median. Printed in alignment.txt, stored as `turn_axis_m` in map_meta.json.
- **`drive_map --video` → `map/map.mp4`** (`MapVideo`): 1120×480, 30 fps paced on t_capture (real time,
  repeats across frames the camera dropped). Left: raw frame + DA floor overlay + contacts + YOLO boxes
  with class/conf/distance; top bar shows how many frames old the YOLO/DA result is. Right: the map as it
  grew (fixed window = map.png's), trajectory, hull circle, FOV from the lens, current detections projected,
  landmarks after first sight, t / leg / heading image vs command. Its final map equals build_floor's (tested).
- drive_map `main` split into `load_camera` / `build` → `DriveMap` dataclass / `map_view`; floor helpers
  `floor_lines`, `floor_labels`, `floor_usable`, `integrate_floor_line`; `row_sightings`; `horizontal_fov`,
  `lens_position`, `draw_fov`. map.png now draws the FOV from the lens, not the centre.
- `drawing`: `view_window` (public), `window=` on `render_map`/`map_px`; `floor_overlay` + `trapezoid_outline`
  + `blend_overlay` (blend_floor now uses them: 18 → 9.8 ms per call, 2.5 ms with a cached overlay).
- **`vision/stream_bench.py --run RUN`** → `map/stream.txt`: live stream rates/latencies from the recording
  logs, per-stage timing, a VO stride sweep over the turning legs, needed-vs-reached table.
- Tests: test_drive_map 7/7 (+ check_rotation_axis, check_sweep_rule, check_video), test_vision 10/10,
  test_mapping 9/9 (+ absolute sign check of left_m). Both reviews run (standards 11 fixes, logic 5 fixes).

## Measured (run 20260926_173930)
- User measured the Astra's three lenses 19.5 / 18.5 / 19.5 cm from the robot centre ("9,cm" read as a
  typo). Images: the lens turns about a point 0.139 m behind and 0.063 m right of it, same for left and
  right turns, same for fx 570 and 630 → RGB is the LEFT outer lens (a −0.062 mount is the worst map). With
  (0.185, +0.062) the lateral error is gone (axis y −0.000) but the axis sits **4.6 cm ahead** of the
  measured centre; drift of the centre inside a turn 11.7 → 7.1 cm (3.3 cm with forward 0.139).
- Live (YOLO + DA sharing the GPU): camera 29.6 fps, 15 frames dropped in 13 gaps, driver→Python p95 130 ms;
  YOLO 6.1/s, infer 121 ms median (53 ms before DA started), capture→drawn p95 333 ms; DA 2.4/s, p95 817 ms.
- Alone: VO 56 ms/pair, floor→map 14 ms, render 3.6 ms, live-view frame 15 ms (67 fps).
- VO sweep: steps ≤ 5.4° exact (±0.2 %), 7.7° −4.7 % / 96 % pairs, 9.9° 82 %, 14.9° 5 %.
  Needed at the demo turn (0.24 rad/s real): VO ≥ 2.6 Hz (5.2 with 2× margin = today's stride 5, ~30 %
  of a core), floor ≥ 0.47 Hz, YOLO ≥ 0.7 Hz → all met. At the chassis max (2.05 rad/s real): VO 22–44 Hz,
  DA 4 Hz → not met.

## Decisions
- Default mount = the user's measurement (0.185, +0.062), not the image-measured axis; the 4.6 cm
  discrepancy is reported, not absorbed.
- Old recordings keep their recorded mount; the new offset is applied with `--cam-forward/--cam-left`.

## Pending
- Step 4 (FORWARD leg run with tape-measure ground truth), 8 (`--stride` DA offline), 11 (limited mode
  for the 3 old runs without raw/), 12, 13 — docs must cover `left_m`, `map.mp4`, `stream.txt`,
  `turn_axis_m` in interfaces.md / README, and create `agent/plan/vision_plan.md`.
- VO has not been timed live next to YOLO + DA (YOLO slows 2.3× when DA runs).
- Why the robot turns about a point 4.6 cm ahead of its measured centre (uneven wheel slip, or the
  centre measured from a different point).
