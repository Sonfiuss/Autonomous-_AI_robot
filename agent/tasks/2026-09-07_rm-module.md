---
id: 2026-09-07_rm-module
status: testing
module: RM (project/)
started: 2026-09-07
---

## Task
Implement RM (kinematics logic layer) in `project/` as an independent, ESP32-embeddable C++ library:
omni_kinematics, velocity_profile, odometry, driver_stepdir + constants.h + PC build/test.

## Input
- Spec: `documents/software/RM/omni3wheel.md` (paper Eq.16 IK; Eq.17 FK in doc is WRONG — user agreed to use the true inverse).
- Chassis params: use the OLD robot values from `agent/description/project_overview.md`
  (a=0.055 m, L=0.21 m, wheels at 60/180/300°, 200 full steps × 64 microstep = 12800 steps/rev).
  The 5 cm / 15 cm / 0-120-240° / PPR100 table given at intake was only an assumption — superseded.
- No encoder on the robot yet → odometry input = motor steps actually commanded (COUNTS_PER_REV = 12800).
  Slip detection deferred (impossible without encoder).
- User decisions: both profile modes (slew limiter for streaming `M` cmds + trapezoidal for distance moves),
  accel/decel default 2 rad/s², MAX_PULSE_HZ 20 kHz placeholder, driver_stepdir = pure conversion (no GPIO),
  sign convention: x forward, y left, θ CCW+, wheel ω+ pushes robot CCW-tangential.
- Embedding constraints: float only, no heap, no exceptions, no iostream/STL containers, dt passed in by caller.
- Firmware (UART parse, pulse generation, control loop) will be written later by user.

## Expected output
- `project/config/constants.h`, `project/include/RM/*.h`, `project/src/RM/*.cpp`, `project/CMakeLists.txt`
  (PC static lib + tests; doubles as ESP-IDF component), `project/library.json` (PlatformIO), `project/tests/test_rm.cpp`.
- `g++ -std=c++14 -Iinclude -Iconfig src/RM/*.cpp tests/test_rm.cpp -o test_rm && ./test_rm` passes:
  IK∘FK identity, frame transforms, slew limiter obeys accel + keeps direction, trapezoid/triangle profiles,
  odometry square-walk returns to origin.

## Plan
- [x] 1. Remove empty dir `project/config/constants.h`; write `constants.h` (old chassis params + derived + limits).
- [x] 2. `include/RM/types.h` (BodyVel, GlobalVel, WheelSpeeds, Pose) + `rm_debug.h` (RM_DLOG gate).
- [x] 3. `omni_kinematics.h/.cpp`: general matrix IK from wheel angles, FK = 3×3 inverse, global↔body.
- [x] 4. `velocity_profile.h/.cpp`: `VelocityProfile` (slew limiter, proportional scaling) + `TrapezoidalProfile`.
- [x] 5. `odometry.h/.cpp`: counts→Δφ→FK→midpoint-heading integration, wrap θ, velocity estimate.
- [x] 6. `driver_stepdir.h/.cpp`: ω↔(Hz,dir) with clamp/deadband, angle/distance↔steps, `StepAccumulator`.
- [x] 7. `CMakeLists.txt` (PC + ESP-IDF component switch), `library.json`, `README.md` (embed guide).
- [x] 8. `tests/test_rm.cpp`; compile with MinGW g++ and run.
- [x] 9. Run /code-standards-review then /code-logic-review; update agent/plan/rm_plan.md, history, action log.

## Execution log
[23:11] step 1: removed empty dir project/config/constants.h | info: skeleton .h/.cpp files were all 0 bytes; no cmake on this PC, only MinGW g++ 6.3 → tests built with direct g++ call

[23:14] steps 2-7: all headers/sources, CMakeLists (IDF switch), library.json, README written | info: added types.h + rm_debug.h beyond skeleton
[23:15] step 8: tests/test_rm.cpp — g++ -Wall -Wextra -Wpedantic clean, ALL RM TESTS PASSED | info: odometry square-walk closes within 1 cm using StepAccumulator counts

[23:20] standards-review: 2 violations fixed (named test tolerances, Odometry::valid() so a singular matrix is detectable), 0 deferred | info: DriverStepDir kept as static-method class to match skeleton file naming

[23:30] logic-review: 3 mismatches fixed (odometry no longer drops counts on tiny dt, O(1) fmod wrapAngle, README notes O theta in deg) | risk: frame of `M vx vy w` unspecified in interfaces.md — old firmware = body frame, RM spec = world frame; README documents both, user must pick
[23:31] step 9 done → status testing. Verify: cd project && g++ -std=c++14 -Wall -Wextra -Iinclude -Iconfig src/RM/*.cpp tests/test_rm.cpp -o test_rm && ./test_rm

## Test result
