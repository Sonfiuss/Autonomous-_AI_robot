---
id: 2026-09-25_drive-map
status: executing   # user duyệt plan 2026-09-26 16:09
module: vision (+ communication)
started: 2026-09-25
---

## Task
Dựng bản đồ 2D của vùng robot đã đi qua từ một lần chạy `demo_drive`: ảnh từng frame + sàn trống
(Depth Anything) + khoảng cách vật (YOLO), đặt vào khung thế giới bằng chuyển động của robot (giả
thiết KHÔNG trượt), và dùng chênh lệch giữa các frame liền kề để khớp thời gian / kiểm tra / ghép bằng chứng.

## Input
- Thư mục chạy `vision/output/<run_id>/`: `run.json` (plan F/T, mốc status từng leg), `detections.jsonl`
  (mỗi frame: t, detections có box + range, floor tóm tắt), `detections.mp4` (ảnh ĐÃ vẽ overlay).
- Giả thiết user: bánh không trượt → quãng/góc mỗi leg = lệnh (profile hình thang của MC/RM).
- Mount camera: cao 0.24 m, pitch 0 (chưa đo trên robot), intrinsics RGB chưa hiệu chuẩn (fx≈570 mượn từ FOV IR).

### Sự thật đã kiểm (15:00)
- Output hiện tại KHÔNG đủ để vẽ map: sàn trống chỉ lưu dạng tóm tắt (`free_pct`, `clear_ahead_m`),
  không có mask/điểm chạm theo pixel; ảnh trong mp4 đã bị tô overlay (sàn xanh/đỏ, box, thanh chữ);
  không có thời điểm chụp của frame mà DA đã phân tích (kết quả DA trễ ~0.3–0.45 s).
- Mốc status trong `run.json` ≠ lúc xe thật chạy: optical flow giữa frame liền kề (run 135710) cho thấy
  leg 1 status 0.17 s nhưng ảnh bắt đầu chuyển động ~2.5 s (READY_WAIT_MS 2 s + trễ), leg 2 status 5.49 s
  → quay thật 5.9–9.3 s. Thời lượng chuyển động khớp MC (3.0 vs 3.2 s, 3.4 vs 3.5 s).
- Tổng yaw đo từ flow ngang = ~73° cho lệnh 90° → fx 570 có thể sai (FOV RGB rộng hơn) hoặc LK hụt khi
  dịch lớn; với giả thiết không trượt, leg quay chính là phép đo fx.
- ~~Odometry firmware (`O`) sai: `pose 0.900 0.900 -0.0deg` sau F0.3/T-90/F0.6~~ → chẩn đoán SAI, xem
  mục 2026-09-26 15:00. Kết luận "không dùng O làm pose" vẫn giữ, nhưng vì lý do khác.

## Expected output
1. `python3 vision/drive_map.py --run vision/output/<run_id>` tạo `vision/output/<run_id>/map/`:
   `map.png` (trống trắng / vật cản đen / chưa biết xám, quỹ đạo robot, vị trí từng frame, vật có nhãn
   + khoảng cách), `map_grid.npy` + `map_meta.json`, `scene.json` (v2.0 như map_builder), `trajectory.csv`
   (t, x, y, θ mỗi frame), `objects.json`, `alignment.txt` (mỗi leg: độ trễ khởi động, lệnh vs đo từ ảnh).
2. Lần chạy MỚI (sau khi sửa recorder) cho map: đi 30 cm → rẽ 90° → đi 60 cm, quỹ đạo khớp lệnh,
   vật/tường đứng yên nằm cùng một chỗ khi nhìn từ các frame khác nhau (không bóng ma).
3. Ba run cũ: tool vẫn chạy ở chế độ hạn chế (quỹ đạo + khớp thời gian + vật), báo rõ "không có ảnh gốc
   → không có sàn trống".
4. `--video`: `map.mp4` ảnh camera cạnh map đang lớn dần theo thời gian.
5. Test offline không cần camera/model pass (`vision/test_drive_map.py`).

### Quyết định của user (15:20)
- Có **một luồng riêng lấy ảnh gốc**; frame của mọi luồng xử lý (ảnh gốc, YOLO, Depth Anything, video)
  phải map **chính xác** với nhau → một số thứ tự `seq` chung gắn ngay lúc camera trả frame.
- **Góc quay tin ảnh hơn bánh xe**: yaw lấy từ chênh lệch frame liền kề; quãng tiến vẫn theo lệnh
  (không trượt) nhưng được VO sàn kiểm tra (memory 06-29: lệnh 30 cm → đo 22 cm, 73%).
- Camera cách tâm xe **18 cm** (giả định: phía trước, trên trục +x) → `forward_m = 0.18`.

### Sự thật đã kiểm (2026-09-26 01:19) — trước khi lập lại plan
- Astra Pro RGB mở được trên `/dev/video0` (640x480, 30 fps); ESP32 trên `/dev/ttyUSB0`; không có
  recorder / robot_link nào đang chạy. Có màn hình local `:0` (cửa sổ OpenCV dùng `DISPLAY=:0`).
- **Phòng đang tối**: độ sáng TB frame = 4/255 sau 120 frame, chỉ 222 ORB keypoint. Camera không có
  điều khiển exposure (chỉ brightness/gamma/contrast) → phải BẬT ĐÈN trước mọi lần ghi.
- `WHEEL_RADIUS_M` vẫn 0.055 trong khi memory 06-29 đo r_eff ≈ 4.03 cm (30 cm lệnh → 22 cm thật) → quãng
  "không trượt" có thể lệch hệ thống ~27 %; cần một phép đo thước dây trên lần chạy mới.
- Không có Astra depth trên Jetson → `analyze_floor` không đo được pitch (cần contact có depth) → pitch
  phải đo riêng; sai 1° pitch ở 2 m ≈ ±15 % khoảng cách sàn.

### Phản hồi user + sự thật đã kiểm (2026-09-26 15:00)
- **Ánh sáng đã cải thiện** (user). Đo lại trên `/dev/video0`: độ sáng TB 105.6/255, 1978 ORB (trần 2000)
  → đạt ngưỡng bước 2. Camera đang nhìn thẳng vào chân ghế ~0.5 m, sàn gạch xám lốm đốm → dọn chỗ
  trống trước khi hiệu chuẩn; dấu sàn dùng băng dính đen/màu đậm.
- **r_eff = 4.03 cm** (user ghi nhận) → `distance_scale = 4.03 / 5.5 = 0.733`. Firmware tính số bước bằng
  r = 5.5 cho CẢ `F` và `T`, nên góc quay thật cũng ×0.733 nếu L = 21 cm đúng (T 90 → ~66°). Ảnh đo ~73° với
  fx 570 mượn → fx và góc thật ràng nhau (quay 66° ⇒ fx ≈ 630; quay 90° ⇒ fx ≈ 460); bước 3 đo fx độc lập
  để gỡ. Số 4.03 đo 06-29 trên firmware Arduino cũ — 4b xác nhận trên firmware ESP-IDF.
- **Odometry `O` không tính sai**: `--run-plan` không gửi `R`, nên O chỉ về 0 khi ESP32 khởi động lại. Run
  134324 và 135710 kết thúc (0.30, 0.60, 90°) = đúng lệnh; run 135826 bắt đầu từ chính pose đó (ready 0,
  không reboot) → F0.3 ở 90° → (0.30, 0.90), T−90 → 0°, F0.6 → (0.90, 0.90, 0°) — khớp chính xác.
  Không dùng O làm pose vì: (1) tích phân từ bước ĐÃ LỆNH (không encoder) → trùng quỹ đạo bước 5, không
  thấy trượt; (2) dùng r = 5.5 → quãng và góc đều lớn hơn thật ~36 %; (3) robot_link chỉ log pose cuối.

### Kiểm tra bằng 5 vật (2026-09-26 15:49) — `vision/output/calib_20260926_objects/`
- User: h = 0.24 m chính xác; 5 vật ở 0.91 / 1.58 / 2.14 / 2.50 / 2.94 m (từ camera). Mốc chạm sàn = vạch
  bóng tiếp xúc ở chân chai (sàn bóng); gói dẹt lấy giữa (v 401–438). Chai sát tủ (u 206) giả định không
  thuộc 5 vật (ra 1.90 m) — chờ user xác nhận thứ tự ghép.
- Tham số hiện tại (pitch 0, f 570) đo **ngắn hơn thật 16–35 %**, tăng theo khoảng cách (2.94 → 1.91 m).
- Fit pitch với f = 570: **pitch = −2.5°** (camera ngửa lên, chân trời ở v ≈ 264 chứ không phải 240) → sai
  −4.7…+9.4 cm, RMS 2.7 %. Đo "từ tâm xe" (−0.18) fit tệ hơn (RMS 7.8 %) → số đo là từ camera.
- fx KHÔNG xác định được từ dữ liệu này (các vật gần giữa ảnh, chỉ có khoảng cách): f 570/pitch −2.5 và
  f 602/pitch −2.1 khớp như nhau → vẫn cần 2 dấu ngang ±0.4 m (bước 3) hoặc vòng 360° (4a) cho fx.
- Độ nhạy: ngoài 2 m, 1 px ≈ 5–6 cm; sai pitch 0.2° → vật 2.94 m đọc 2.81–3.06 m → map nên tin sàn
  gần (≤ ~2.5 m) hơn, và rung pitch khi xe tăng/giảm tốc sẽ làm nhoè phần xa.

- Lần 2 (16:02, camera đã được chỉnh): cả ảnh dịch lên đều 15.5–16.5 px, vật không đổi chỗ → chỉ pitch
  đổi (+1.6°), chiều cao giữ. Fit lại: **pitch = −0.92°**, RMS 2.2 %, cùng mẫu sai số từng vật như lần 1
  (ngọc +8 cm, hồng −5 cm → lỗi mốc/đo của vật đó, không phải mô hình). Pitch 0 giờ sai −8…−17 %;
  giữ −2.5° cũ thì sai +7…+50 % → **giá camera không cứng: chạm vào là phải đo lại pitch**.

## Plan (lập lại 2026-09-26 — bước 1 giữ nguyên, các bước còn lại chia 2 pha)

### Pha A — cần camera cắm (+ robot), làm TRƯỚC khi rút camera
- [x] 1. Luồng ảnh gốc + `seq` chung:
      a. `frame_grabber`: `Frame` thêm `seq` (đếm mỗi frame nhận, không lặp lại qua reopen) và `wall`
         (`time.time()` lúc nhận, cùng thời điểm với `t`); `listener` gọi cho MỌI frame trên thread grabber.
      b. `vision/raw_recorder.py` `RawFrameWriter`: thread riêng ghi mọi frame camera (30 fps) →
         `raw/<seq>.jpg` + `raw/index.jsonl` (seq, t_capture, t_mono). Hàng đợi có giới hạn, tràn → bỏ + đếm;
         `ensure(frame)` ghi đồng bộ một frame YOLO/DA đã dùng mà bị bỏ → frame nào đã xử lý đều có trên đĩa.
      c. `record.py`: dòng jsonl có `seq`, `t_capture`, `depth_seq`, `video {first, count}`; FloorWorker mang
         `seq` của frame DA đã phân tích → `floor/index.jsonl` (seq, t_capture, plane, pitch, contacts, %)
         + `floor/<seq>.png` (nhãn pixel); `recording.json` (mount, intrinsics, floor cfg, fps, chất lượng JPEG).
      d. `DEFAULT_MOUNT.forward_m = 0.18`; `clear_ahead` vẫn đo từ camera; `record.py --cam-forward`.
      e. Test: seq/wall/listener của grabber, RawFrameWriter (tràn + ensure + index) trong `test_vision.py`;
         chạy thật ~10 s trên camera, đối chiếu seq giữa raw / jsonl / floor.
- [x] 2. Kiểm tra ánh sáng trong recorder: `record.py` đo độ sáng TB + số ORB keypoint của ~1 s đầu, ghi vào
      `recording.json` (`light: {mean, orb}`), log WARNING khi < ngưỡng (`MIN_MEAN_BRIGHTNESS` ~40,
      `MIN_ORB` ~500). Không chặn ghi — chỉ để lần chạy tối không bị nhầm là lỗi thuật toán.
      Kiểm: chạy `record.py --no-floor --max-seconds 5` lúc tắt đèn → có warning; bật đèn → không.
- [~] 3. (code xong 16:25; pitch đã có từ 5 vật; chờ user dán dấu để chốt fx) Hiệu chuẩn TĨNH camera trên robot bằng dấu băng dính trên sàn — `vision/calib_floor.py`
      (thay cho 4b cũ: cho cả fx LẪN pitch, không cần robot chạy).
      a. User: đo chiều cao tâm ống kính so với sàn; dán 5 dấu, toạ độ đo từ điểm sàn ngay dưới ống kính
         (tiến, trái): (0.8, 0), (1.2, 0), (2.0, 0), (1.2, +0.4), (1.2, −0.4) — file `marks.json` mặc định có sẵn.
      b. Tool: lấy trung bình 10 frame (bớt nhiễu), hiện cửa sổ (`DISPLAY=:0`) để click lần lượt từng dấu;
         không có màn hình thì `--points u,v ...`; luôn lưu `calib/frame.png` để làm lại offline.
      c. Fit bình phương tối thiểu: fx (= fy, pixel vuông), pitch; cx, cy giữa ảnh (5 điểm không đủ tin để fit);
         height lấy số đo, fit lại chỉ để so. In residual từng dấu (px và cm trên sàn).
      d. Ghi `vision/camera_rgb.json` {fx, fy, cx, cy} (định dạng `--intrinsics` sẵn có) + `vision/camera_mount.json`
         {height_m, pitch_deg, forward_m}; `record.py` / `drive_map.py` tự đọc 2 file này khi có (cờ vẫn ghi đè).
      e. (bổ sung 15:00) Gốc O = điểm dây dọi từ tâm ống kính **RGB** (không phải lỗ IR); trục X = đường qua
         điểm chạm bánh trước W1 và trung điểm W2–W3; servo pan/tilt về 0 (`robot_link --reset`) và giữ nguyên;
         dán dấu vị trí 3 bánh để đặt lại xe; dấu kiểm tuỳ chọn (1.60, −0.20) không dùng để fit, chỉ báo sai số.
         Tool báo thêm độ lệch yaw của camera (fit tham số thứ 3, tương đương lệch cx).
      Kiểm: residual trung bình < 3 cm ở 1.2 m; test fit trên điểm sinh từ camera giả biết trước fx/pitch.
- [ ] 4. Ghi dữ liệu thật với robot (USER chạy, bật đèn, tôi chuẩn bị lệnh + kiểm output ngay sau mỗi lần):
      a. Vòng quay: ~~"quay trái 360 độ"~~ → **`"quay trái 270 độ, quay trái 270 độ"` (540° lệnh)** (16:33: nếu r_eff 4.03
         đúng, 360° lệnh chỉ quay ~264° thật → không khép vòng; 540° lệnh ≈ 396° thật vẫn đi qua góc nhìn đầu; một
         bước quay bị giới hạn 1..360°). Frame giống frame đầu nhất = đúng 360° thật → Σyaw ảnh ở đó kiểm fx, và góc
         lệnh (profile) ở đó = tỉ số quay thật/lệnh. User dán vạch băng dính dọc thân xe lúc đầu, đo góc cuối so vạch.
      b. Lần chạy nghiệm thu: `"đi 30 cm, rẽ trái 90 độ, đi 60 cm"`; user đánh dấu vị trí đầu/cuối tâm xe và
         đo thước dây quãng thật (dx, dy) + góc cuối → ground truth cho quãng "không trượt" (r 5.5 vs 4.03 cm).
      c. (tuỳ chọn) Lần chạy phủ phòng: quay 360° tại chỗ + 2–3 leg + quay 360° lần nữa.
      Kiểm ngay sau mỗi lần: `recording.json` counts (raw_dropped = 0), seq raw liên tục, floor/ có file,
      độ sáng đạt. Sau bước này camera có thể rút — pha B chạy hoàn toàn offline trên các thư mục này.

### Pha B — offline trên dữ liệu vừa ghi (không cần camera / robot)
- [x] 5. `vision/drive_timeline.py` (thuần): từ `run.json` dựng quỹ đạo không trượt — mỗi leg F/T → profile
      hình thang theo RM (qua `mc_client` + `project/build/mc/libmc.so`, cùng chuỗi với firmware; fallback công
      thức hình thang Python nếu thiếu lib), neo tại `start_k`; `pose(t)` nội suy cho mọi frame. Tham số
      `--distance-scale` (mặc định 0.733 = r_eff 4.03 / 5.5, user 2026-09-26; thay bằng số đo 4b khi có).
- [x] 6. `vision/frame_motion.py` — chênh lệch giữa frame liền kề trên ảnh GỐC (`raw/<seq>.jpg`):
      (a) yaw: flow ngang của feature phía trên đường chân trời, Δθ = atan((u1−cx)/fx) − atan((u0−cx)/fx);
      (b) tịnh tiến: feature trên sàn → toạ độ sàn (height + pitch bước 3) → fit cứng 2D RANSAC → Δx, Δy, Δθ (m).
      ORB + LK nhiều tầng (chịu dịch ~80 px khi quay). Run cũ (ảnh đã vẽ overlay): che overlay → chỉ còn yaw.
      Kiểm trên run 4a: Σyaw ≈ 360° ± 5° và khép vòng.
- [x] 7. Khớp thời gian + pose (5 + 6): mỗi leg tìm `start_k` trong [status_k, status_{k+1}) cho vận tốc profile
      khớp chuyển động đo được nhất. **Yaw = tích luỹ yaw ảnh** (user tin ảnh hơn bánh); quãng tiến = lệnh ×
      scale, VO sàn kiểm tra. `alignment.txt`: trễ mỗi leg, góc ảnh vs lệnh, quãng VO vs lệnh vs thước dây.
- [~] 8. (chưa: DA offline cho frame khác `--stride`) `vision/drive_map.py` — ghép map sàn: mỗi frame có `floor/<seq>.png` (DA đã chạy lúc ghi) → đọc lại
      nhãn; frame raw khác chạy DA offline khi `--stride` yêu cầu (cache `map/da/<seq>.npy` float16) →
      `free_floor_xy` + contact → thế giới bằng `pose(t_capture)`. Bỏ frame |ω| > ngưỡng (nhoè). Log-odds
      theo frame nhỏ hơn map_builder: ô chỉ đổi trạng thái khi ≥2–3 frame đồng ý → `integrate_floor` nhận
      trọng số tham số (mặc định giữ nguyên cho map_builder).
- [x] 9. Vật: detection → điểm chạm sàn (`box_floor_range`, fx/pitch bước 3) → thế giới; gom theo lớp +
      khoảng cách < 0.3 m thành landmark (trung vị vị trí, số frame, conf TB); giữ landmark thấy ≥3 frame;
      phiếu nhãn vào map để `scene_export` gắn nhãn cụm vật cản.
- [x] 10. (`--video` xong 18:45) Xuất `vision/output/<run>/map/`: `map.png` (render_map + quỹ đạo + landmark), grid/meta/`scene.json`
      (tái dùng `export` của map_builder), `trajectory.csv`, `objects.json`, `alignment.txt`; `--video` → `map.mp4`.
- [x] 10b. (user 2026-09-26 18:30) Vị trí camera đo tay: ống kính Astra cách tâm xe 19.5 / 18.5 / 19.5 cm (3 ống
      kính; "9,cm" coi là gõ nhầm) → ống giữa trên trục +x ở 18.5 cm, hai ống bên lệch ±6.2 cm. Ảnh (vòng quay
      173930) đặt trục quay 13–14 cm sau và **6.1–7.0 cm bên PHẢI** ống RGB (quay trái và quay phải như nhau, fx 570
      hay 630 như nhau) → RGB là ống bên TRÁI xe: `CameraMount.left_m` mới (mặc định 0 → recording cũ đọc được),
      DEFAULT_MOUNT forward 0.185 left +0.062; floor_hit / floor_project / camera_to_body / box_floor_range /
      floor_trapezoid / calib_floor / renderer test xử lý left_m; `drive_map --cam-forward/--cam-left` (recording cũ
      ghi mount cũ); alignment.txt in trục quay đo từ ảnh để so với số đo tay.
- [x] 10c. `drive_map --video` → `map/map.mp4` theo thời gian thật (mỗi raw frame): trái = ảnh gốc + box YOLO
      (lớp, conf, khoảng cách "~x.xx m") + phủ sàn DA (kết quả mới nhất có seq ≤ frame, in tuổi theo frame),
      phải = map lớn dần (khung nhìn cố định = khung map cuối), quỹ đạo tới lúc đó, pose + FOV hiện tại, vật
      của frame hiện tại chiếu lên map, landmark; thanh trên: t, leg, heading ảnh vs lệnh.
- [x] 14. (user) Đo streaming — `vision/stream_bench.py --run RUN`: (a) từ log lần ghi: fps + độ trễ thật của
      camera / YOLO / DA / video; (b) đo thời gian từng khâu của map trên chính frame đã ghi (VO theo stride,
      tích hợp sàn, vẽ map, ghi video) → fps tối đa mỗi khâu; (c) tần số TỐI THIỂU để realtime: VO theo tốc độ
      quay (bước ≤ ~3° đo được tốt, 11° hỏng), DA/YOLO theo độ phủ → bảng "cần / đạt" ghi `map/stream.txt`.
- [~] 11. (7/7 pass; chưa: chế độ hạn chế cho 3 run cũ không raw/) Test `vision/test_drive_map.py` (không camera/model): profile leg đúng quãng/góc; khớp thời gian tìm lại
      trễ đã cài (±50 ms); VO sàn ra đúng Δpose trên cảnh giả; vật đứng yên nhìn từ 5 pose → 1 landmark;
      `calib_floor` fit lại đúng fx/pitch; chạy thử trên 3 run cũ (chế độ hạn chế, báo "không có ảnh gốc").
- [ ] 12. (tuỳ chọn) `communication/demo_drive.py --map`: dựng map ngay sau khi chạy xong.
- [ ] 13. Docs: `vision/README.md` (hiệu chuẩn sàn + dựng map từ lần chạy), `interfaces.md` (trường jsonl /
      recording.json mới, camera_*.json), tạo `agent/plan/vision_plan.md`; `/code-standards-review` +
      `/code-logic-review`.

## Execution log
<!-- One line per event. Format: [HH:MM] step N: <action> | risk: <note> | info: <key fact> -->
[15:07] intake | info: đọc run 134324/135710/135826; flow liền kề xác nhận trễ leg + fx nghi sai; odometry O firmware sai | risk: cần một lần chạy robot MỚI (sau bước 1) mới có map sàn trống thật
[01:00] step 1: frame_grabber (seq/wall/listener + t_sensor = stamp V4L2 qua CAP_PROP_POS_MSEC, capture_wall), raw_recorder.RawFrameWriter, record.py (seq/t_capture/depth_seq/video trong jsonl, FloorLog floor/<seq>.png + index, recording.json), forward_m 0.18; test_vision 8/8 | info: camera thật 12 s: raw 333 frame seq 0–332 liên tục, 0 drop; mọi seq YOLO (69) / floor (27) có trong raw, t_capture khớp 0.0; dt capture median 32 ms (max 100 ms: camera tự bỏ frame); arrival − stamp driver ~35 ms (max 209 ms khi YOLO+DA giữ CPU) → dùng t_capture chứ không dùng arrival | risk: overlay sàn trễ ~20 frame (0.65 s) so với YOLO — map dùng floor/<seq> theo seq của chính nó nên không ảnh hưởng

[01:25] re-plan | info: camera /dev/video0 OK nhưng phòng tối (mean 4/255, 222 ORB); không Astra depth → pitch đo tĩnh bằng dấu sàn (bước 3 mới, thay 4b); bước 2–10 cũ → 5–13, thêm pha A ghi dữ liệu khi camera còn cắm | risk: quãng "không trượt" có thể lệch ~27 % (r 5.5 vs 4.03 cm) → cần đo thước dây ở 4b
[15:00] feedback | info: đèn OK (mean 105.6, ORB 1978); r_eff 4.03 → scale 0.733 cho F và cả T; O không sai — 135826 không reset, cộng dồn từ 135710 | risk: 4.03 đo trên firmware cũ; camera đang bị ghế che ~0.5 m
[15:49] check 5 vật | info: pitch 0 đo ngắn 16–35 %; fit pitch −2.5° (f 570, h 0.24) → sai ≤ 9 cm, RMS 2.7 %; fx chưa xác định được | risk: thứ tự ghép vật do mình suy, chờ user xác nhận
[15:52] review gate | info: không có source project nào đổi (git status: chỉ file task); gate bật do script phân tích tạm `fit_objs.py` trong scratchpad /tmp → không chạy /code-standards-review, /code-logic-review
[16:02] check 5 vật lần 2 | info: camera bị chỉnh, ảnh dịch đều −15.7 px → pitch −2.52° → −0.92° (RMS 2.2 %), cao không đổi | risk: giá camera không cứng — pitch phải đo lại mỗi khi chạm vào camera
[16:09] approved | user: "sử dụng pitch 0.92, approve kế hoạch" → `DEFAULT_MOUNT.pitch_deg = -0.92` (dấu âm = ngửa lên theo quy ước code; giá trị fit ở 16:02) | info: test_vision 8/8, test_mapping 9/9 vẫn pass
[16:13] fix (ngoài plan, chặn bước 3/6/8) | `floor_hit` + `camera_to_body` dùng forward = z cos p **+** y sin p — phép xoay không trực chuẩn, chỉ pitch 0 che được; ở 10° đặt điểm 2.94 m ngắn 9.5 cm. Sửa thành **−**; `fit_floor` bỏ hệ số 1/cos 2φ (sinh ra từ lỗi đó); renderer `_trace` của test_mapping cùng lỗi → sửa + thêm kiểm bằng góc (h/tan θ) trong check_geometry (công thức cũ bị bắt 9 lỗi) | info: round-trip 1e-15 m; fit_floor tìm lại đúng h/pitch; tường check_map lệch ≤3 cm (trước 7 cm); fit 5 vật lại: −0.90° (lần 2) / −2.44° (lần 1), RMS 2.0 % — giữ −0.92 theo user | risk: `measure_pitch` dùng quan hệ độ cao nên đúng sẵn
[16:18] step 2: `vision/light_check.py` LightCheck (bỏ 60 frame đầu cho auto-exposure, trung vị 10 frame YOLO, ORB cap 2000), `record.py` gọi mỗi cycle + `recording.json.light`, warning khi < 40/255 hoặc < 500 ORB; test_vision check_light (sáng/tối/skip/sau-done) 9/9 | info: chạy thật 6 s: light {mean 104.5, orb 1926, ok}, raw 149/0 drop, mount.pitch −0.92 | risk: chưa thử thật lúc tắt đèn (test tổng hợp đã phủ)
[16:25] step 3 (code): `vision/calib_floor.py` (trung bình 10 frame sau 60 frame warm-up; click có kính lúp ×8 / `--points` offline; LM trên sai số pixel: pitch luôn, fx+cx chỉ khi ≥2 dấu có y cách ngang ≥0.3 m; h tự do in ra để so; dấu `check` giữ ngoài fit; PASS khi sai số sàn TB ≤ 2.5 % khoảng cách; ghi OUT/frame.png, calib.json, check.png + `vision/camera_mount.json` / `camera_rgb.json`), `calib_marks.json` (5 dấu + dấu kiểm), `floor_segment.floor_project` (nghịch đảo chính xác floor_hit), `floor_geometry.default_mount()` + `object_distance.default_intrinsics_file()` / `fallback_intrinsics()` (FALLBACK_FOV_DEG dời từ record.py) → record.py tự đọc file hiệu chuẩn; test: check_calib_fit (tìm lại fx 600/cx 330/pitch 3.0 từ điểm xuất phát fallback; dấu chỉ-khoảng-cách không fit fx; dấu check lệch 30 px không ảnh hưởng), floor_project round-trip; vision 10/10, mapping 9/9 | info: chạy tool offline trên 5 vật (lần 2, mốc chỉ-khoảng-cách): pitch −0.953°, RMS 1.6 px, PASS 1.8 % (fit theo pixel; fit theo % khoảng cách ra −0.90) — `--no-save`, giữ −0.92 của user; grab camera + cửa sổ click (DISPLAY=:0) chạy không lỗi; record.py thật: mount −0.92, intrinsics fallback, light ok | risk: fx vẫn chờ user dán 2 dấu ngang (±0.4 m); click tương tác chưa được người dùng thật thử
[16:30] standards-review (2 pass): 17 sửa, 1 hoãn | calib_floor: số ma → hằng (LM damping, vẽ, phím, tên file, poll, PCT), imwrite + source.close() được kiểm, `--points` sai định dạng báo rõ, docstring points_arg/passes, logger.debug trong fit; floor_segment `MIN_PROJECT_DEPTH_M`; object_distance docstring fallback_intrinsics | info: **lỗi logic bắt được khi sửa**: click không trừ thanh BAR_H → mọi điểm click lệch 14 px (≈1.4° pitch) — sửa qua `display_to_image`/`image_to_display` + test (top-left, round-trip scale 1–3) | hoãn: marks + points là hai list song song đi qua 4 hàm — giữ vì vision viết kiểu hàm + namedtuple; pyflakes sạch, vision 10/10, mapping 9/9
[16:32] logic-review (2 pass, đối chiếu plan bước 2–3, interfaces, history 09-25): 1 sửa, 1 báo user | sửa: mốc chỉ-khoảng-cách = khoảng cách THẲNG phía trước (+x), không phải đường chéo — ghi rõ ở calib_floor + calib_marks.json (đường chéo tới vật lệch 0.4 m ở 1.6 m dài hơn 3 % = cả ngưỡng PASS) | info: bước 2/3 đủ theo plan (lệch có lý do: light bỏ 2 s đầu cho auto-exposure; fit thêm cx = yaw theo 3e); đơn vị/quy ước pitch (+ cúi) + JSON khớp load_intrinsics/load_mount; LM/ORB không nằm trên đường nóng | báo user: map_builder.py vẫn đọc DEFAULT_MOUNT, không đọc camera_mount.json (ngoài phạm vi plan) | `agent/plan/vision_plan.md` chưa có (bước 13 tạo)
[16:33] step 4 chuẩn bị | dry-run 2 lệnh qua `demo_drive.py --dry-run --provider rule`: "đi 30 cm, rẽ trái 90 độ, đi 60 cm" → F0.3/T90/F0.6; 360° chia 3×T120; 540° bị từ chối (1..360/bước) → "quay trái 270 độ, quay trái 270 độ" = 4×T135 | info: recorder do demo_drive gọi mặc định có raw + floor, tự dùng mount −0.92 | chờ user: (tuỳ chọn) dán 2 dấu ±0.4 m cho fx; chạy 4a + 4b
[16:34] review gate | info: hook bật sau khi 2 review đã chạy trong lượt (16:30 standards, 16:32 logic); từ đó không sửa source nào ngoài fix của chính logic-review (đã test lại: vision 10/10, mapping 9/9) → không chạy lại
[17:30] user | "áp dụng fx 570, sai khác review sau" → `vision/camera_rgb.json` fx=fy=570 (cx 319.5 cy 239.5); yêu cầu test ngay: quay trái 180° + quay phải 180°, dựng map trong lúc quay, show map → thay 4a/4b bằng lần chạy này, làm luôn bước 5–10
[17:37] step 4 (run 1) | `demo_drive --yes --provider rule --yaw-rate 0.3 "quay trái 180 độ, quay phải 180 độ"` → FAILED ở leg 1: firmware E2 (ký tự lệnh lạ), 0 ack, xe không chạy | info: `robot_link --stop` + `--monitor` 4 s: telemetry bình thường, không E mới, undecodable=2 → nghi một dòng dở dang còn trong buffer RX của ESP32 dính vào `T` đầu tiên | risk: `--run-plan` không xả dòng trước lệnh đầu → nên gửi `S`/"\n" trước plan (module motivation, báo user)
[17:39] step 4 (run 2) | cùng lệnh → complete, 4 ack; run `vision/output/20260926_173930`: raw 851 (seq 0–850 liền, 0 drop), floor 67 (62 có plane), YOLO 172, light 111/1829 ok, intrinsics từ file
[17:45] step 5 | `vision/drive_timeline.py`: load_run (legs + status + cruise/yaw từ run.json), profile từ MC (`mc_client.run`, dt 0.02) hoặc trapezoid fallback (lệch MC ≤ 0.008 rad), `displacement`, `align_leg` (tương quan chuẩn hoá tốc độ lệnh vs đo) | info: trễ leg 1 = 2.25 s (READY_WAIT 2 s), leg sau 0–0.2 s, score 0.94–0.998
[17:50] step 6 | `vision/frame_motion.py`: Shi-Tomasi + LK 3 tầng + kiểm tiến-lùi trên vùng sàn ≤ 2.5 m → điểm sàn trong khung thân (floor_hit) → RANSAC cặp 2 điểm + Gauss-Newton trên sai số pixel → (dx, dy, dθ) tâm xe | info: 850/850 cặp đo được, ~300 inlier, rms 0.2–0.4 px; dx,dy < 1 mm/frame → mô hình camera 0.18 m trước tâm quay đúng cỡ
[17:58] step 6 (sửa) | bước 1 frame (~0.5°) cộng thiếu ~1 % (leg 1: 72.7° vs 73.5° ở stride 10; stride 20 fail) → `drive_map` đo cách 5 frame, chia đều cho frame giữa, fail → đo từng frame | info: heading cuối +6.56° = so thẳng frame đầu–cuối 6.45–6.59° (trước đó 5.9°); 14 s cho cả map
[18:00] step 7–10 | `vision/drive_map.py`: align (khoảng leg nối tiếp không chồng — trước chồng nhau làm leg đếm trùng), pose = heading odometry + quãng tiến lệnh × 0.733, floor/<seq>.png + contacts → OccupancyMap.integrate_floor(weight 0.5) (bỏ frame quay > 0.8 rad/s), YOLO chân hộp → landmark (≤0.3 m, ≥3 lần), xuất map.png (quỹ đạo, FOV đầu/cuối mỗi leg, landmark, legend), views.png, map_grid.npy, map_meta.json, scene.json, trajectory.csv, objects.json, alignment.txt, motion.npz (cache); `occupancy_map.integrate_floor(weight=1.0)`; `drawing.map_px` + `render_map(extent_points)`; `vision/test_drive_map.py` 4/4 (trapezoid, align ±50 ms, odometry sàn giả 3 chuyển động, 1 vật từ 5 pose → 1 landmark) | info: **quay thật / lệnh = 0.79** (4 leg 0.75–0.81) với fx 570 — nếu fx ≈ 630 thì = 0.73 = r_eff 4.03/5.5 (chưa tách được); trái 145.4° vs phải −139.1° → xe dừng lệch +6.5° CCW (thật: so frame đầu–cuối); landmark bình xịt tại 2.90 m từ camera (user đo 2.94); floor 62/67 dùng; tâm xe odometry dịch 7–12 cm trong mỗi leg rồi về ~1.5 cm → trục quay thật lệch vài cm so với giả định | risk: test synthetic ban đầu fail do vân sàn răng cưa (σ 2 texel) — đổi σ 8, ghi lý do
[18:07] standards-review (2 pass): 12 sửa, 0 hoãn | drive_map: dict kết quả leg → `@dataclass LegFit`, tuple sighting → `Sighting`, cột mảng odometry `COL_*`, tên file đầu ra → hằng, `np.load` npz trong `with`, số ma (tipLength, lệch nhãn, log mỗi 100 frame, chữ số conf) → hằng, docstring draw_legend/write_alignment/write_trajectory, docstring module (stride, cần raw/), logger.debug align + stride fail, warning labels không đọc được; frame_motion `RANSAC_SAMPLE` + logger.debug; drive_timeline logger.debug | info: kết quả map giống hệt trước refactor; drive_map 4/4, vision 10/10, mapping 9/9
[18:10] logic-review (2 pass; đối chiếu Expected output + bước 5–11, interfaces, history): 5 sửa, 5 báo user | sửa: frame_motion Gauss-Newton dừng khi inlier < MIN_INLIERS (lstsq thiếu hàng); cluster_landmarks O(S·C·m) median mỗi lần so → tâm cụm cache, cập nhật khi thêm (gom theo lớp); write_alignment t0 khi leg đầu không có status; landmark có `range_m` + nhãn "lớp X.XXm xN" (Expected output "vật có nhãn + khoảng cách"); nhãn sát mép phải chuyển sang trái | lệch plan có chủ ý: 6a (yaw từ điểm trên chân trời) thay bằng sàn cho cả 3 bậc — điểm tường 2 m bị thị sai 0.18/d ≈ 9 % do camera lệch tâm quay | chưa làm (báo user): chế độ hạn chế cho 3 run cũ không raw (hiện dừng báo lỗi), `--video`, DA offline cho frame khác (`--stride`), leg FORWARD chưa thử trên dữ liệu thật, bước 12–13 (demo_drive --map, docs, vision_plan.md)
[18:12] review gate | info: hook bật sau khi standards (18:07) + logic (18:10) đã chạy trong lượt; sửa cuối cùng (nhãn landmark tránh mép, `FONT`) nằm trong vòng logic-review, trước dòng log 18:10, đã test lại (drive_map 4/4) → không chạy lại

## Test result
[18:35] intake (user) | vị trí camera đo tay 19.5/18.5/19.5 cm; xin video detect+khoảng cách song song map để đối chiếu; đo streaming + fps cần cho realtime → thêm 10b, 10c, 14 (10c là Expected output 4 đã duyệt) | info: trục quay đo từ ảnh (run 173930, stride 5, trục trong khung sàn camera): camera trước trục 0.139 m, trái 0.063 m (quay trái 0.140/0.061, phải 0.133/0.064; fx 630 → 0.127/0.070) → lệch ngang khớp √(19.5²−18.5²)=6.2 cm | risk: trục quay thật ở ~4.5 cm trước tâm đo tay → xe không quay quanh tâm hình học (trượt không đều), hoặc số 18.5 tính từ điểm khác
[18:40] step 10b: `CameraMount.left_m` (namedtuple default 0 → recording.json cũ vẫn đọc), DEFAULT_MOUNT forward 0.185 left +0.062; floor_hit/floor_project/camera_to_body/box_floor_range/floor_trapezoid/fit_floor; `--cam-left` ở record/calib_floor/map_builder; drive_map `--cam-forward/--cam-left`, `rotation_axis()` → alignment.txt + map_meta `turn_axis_m`; test_mapping kiểm dấu tuyệt đối (cột giữa = left_m) | info: mount (0.18,0) → (0.185,+0.062): trục quay đo được (+0.041,−0.062) → (+0.046,−0.000) — lệch ngang hết hẳn; tâm trôi trong leg quay 11.7 → 7.1 cm; ô vật cản 240 → 228; dấu ngược (−0.062) tệ nhất (18.7 cm, 247) → xác nhận RGB bên TRÁI. Landmark spread 5.0–5.4 cm như nhau mọi mount (bị fx/box chi phối) | risk: trục quay thật ở 4.6 cm TRƯỚC tâm đo tay (lens 0.139 m trước trục, không phải 0.185) — dùng 0.139 thì tâm trôi còn 3.3 cm; giữ số đo tay theo user, báo lại
[18:45] step 10c: `MapVideo` (drive_map `--video` → map/map.mp4, 1120×480, 30 fps theo t_capture, lặp frame qua chỗ camera tự bỏ → đúng thời gian thật): trái ảnh gốc + phủ sàn DA + contact + box YOLO (lớp, conf, "~x.xx m"), thanh trên seq + tuổi YOLO/DA (xanh = đúng frame, cam = giữ từ frame trước, "-N fr") + infer ms, dưới dòng leg; phải map lớn dần (khung cố định = map.png qua `view_window`/`window=`), quỹ đạo, vòng thân xe 0.225 m, FOV từ ống kính (lệch trái), mũi tên heading, dấu + vật của row YOLO hiện tại (pose của chính seq đó), landmark sau first_seq, chữ t/leg/heading ảnh vs lệnh/số ô; refactor main → `load_camera`/`build`/`DriveMap`/`map_view`, `floor_lines/floor_labels/floor_usable/integrate_floor_line`, `row_sightings` | info: run 173930: 863 frame 28.8 s, 40 s tạo (cả map); map cuối video == build_floor (test)
[18:55] step 14: `vision/stream_bench.py` → map/stream.txt | info: LIVE camera 29.6 fps (13 frame camera tự bỏ, driver→Python p95 130 ms); YOLO 6.1/s infer 121 ms (53 ms khi DA chưa chạy) trễ p95 333 ms; DA 2.4/s trễ p95 817 ms; video live 10 fps 39 % frame lặp. STAGES (một mình): VO 55–57 ms/cặp, decode 4.7, sàn→map 14, render 3.6, panel 20+6, encode 1.9 ms. SWEEP 712 frame quay: bước ≤ 5.4° đúng ±0.2 %, 7.7° hụt 4.7 % (96 % cặp), 9.9° 82 %, 14.9° 5 % → cần VO ≥ ω/5.4° (x2 lề): 0.24 rad/s → 2.6 (5.2) Hz = stride 5 hiện tại (6 Hz, ~30 % 1 core); floor ≥ 0.47 Hz, YOLO ≥ 0.7 Hz → đạt; ở 2.05 rad/s (max chassis) VO cần 22–44 Hz (125–250 % core) + DA 4 Hz → KHÔNG đạt | risk: STAGES đo khi không có model chạy song song — VO sống cùng YOLO+DA chưa đo (YOLO chậm 2.3× khi DA chạy)
[18:57] test: vision 10/10, mapping 9/9, drive_map 7/7 (thêm check_rotation_axis, check_sweep_rule, check_video); pyflakes sạch (map_builder `sys` thừa có từ trước)
[19:05] standards-review (2 pass): 11 sửa, 0 hoãn | drive_map: số ma camera_panel (lệch nhãn box, scale chữ, khoảng cách field, màu thanh/dòng motion) → hằng, VIDEO_TEXT_DY thừa bỏ, marker dùng hằng riêng, `horizontal_fov()` thay 3 bản chép công thức (main, MapVideo, stream_bench), `body_to_world` thay 2 phép xoay chép tay (row_sightings, vị trí ống kính), logger.debug trục quay, nhãn leg tách khỏi f-string lồng điều kiện; stream_bench: PCT, tên stage/stream → hằng (VIEW_STAGES thay so chuỗi startswith), floor labels đọc 1 lần, imread None được kiểm ở sweep + timing | info: map/landmark/video/stream.txt giống hệt trước sửa; drive_map 7/7, vision 10/10, mapping 9/9
[19:15] logic-review (2 pass; đối chiếu plan 10b/10c/14 + Expected output 4, interfaces, history 09-26): 5 sửa, 3 báo user | sửa: (1) `blend_floor` dựng lại 4 mặt nạ nhãn + contour hình thang MỖI frame dù phân tích DA đổi ~12 frame/lần → `drawing.floor_overlay` (1 lần/phân tích) + `trapezoid_outline` (1 lần) + `blend_overlay` (addWeighted + copyTo): 18 → 2.5 ms/frame, recorder 18 → 9.8 ms, khác ≤ 1 mức sáng, pixel không nhãn giữ nguyên; (2) quỹ đạo chiếu lại toàn bộ mỗi frame (O(N²)) → `track_px` tính 1 lần (khung cố định); (3) đếm ô free/occ mỗi frame → chỉ khi map đổi; (4) map.png vẽ FOV từ TÂM (render_map + draw_overlays) → từ ống kính (`lens_position`, `draw_fov`), lệch 19.5 cm trước đó; (5) stream_bench đếm khoảng hở thay vì frame bị bỏ (100 ms = 2 frame) → 15 frame / 13 khoảng | info: live view 33 → 15 ms/frame (67 fps 1 core), camera panel 19.9 → 4.4 ms, drive_map+video 40 → 22 s; map/landmark không đổi; drive_map 7/7, vision 10/10, mapping 9/9 | báo user: interfaces.md/README chưa có left_m, map.mp4, stream.txt, turn_axis_m (bước 13); trục quay 4.6 cm trước tâm đo tay; STAGES đo khi không có YOLO/DA chạy song song
[19:25] review gate: standards + logic lại trên phần logic-review 19:15 thêm (drawing.floor_overlay/trapezoid_outline/blend_overlay, drive_map lens_position/draw_fov/cache MapVideo, stream_bench đếm frame bỏ) | 2 sửa, 0 hoãn: docstring blend_overlay ghi 8 ms (bản np.copyto) → 2.5 ms đo thật; dùng giá trị trả về của cv2.copyTo thay vì dựa vào ghi đè tại chỗ | info: so với cách blend cũ: khác ≤ 1 mức sáng, 107560/107560 pixel có nhãn được phủ, pixel không nhãn giữ nguyên; drive_map 7/7, vision 10/10, mapping 9/9, pyflakes sạch
